<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs490.cal_o_meter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".Register" />
        <activity android:name=".ui.home.TestActivity" />
        <activity android:name=".ui.home.BarcodeScanActivity" />
        <activity android:name=".ui.home.BarcodeInputActivity" />
        <activity android:name=".ui.home.PicInputActivity" />
        <activity android:name=".ui.home.SearchByNameActivity" />
        <activity android:name=".ui.home.ManualInputActivity" />
        <activity android:name=".ui.home.SingleItemActivity" />
        <activity android:name=".ui.home.InputOptions" />
        <activity android:name=".ui.home.Calendar" />
        <activity android:name=".ui.recommendations.Recipes" />
        <activity android:name=".ui.recommendations.Restaurant" />
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>